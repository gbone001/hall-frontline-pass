DISCORD_TOKEN=
VIP_DURATION_HOURS=4
CHANNEL_ID=
LOCAL_TIMEZONE=Europe/Berlin  # IANA timezone, e.g. Europe/Berlin

# Storage (JSON file for Discord <-> T17 links)
DATABASE_PATH=                # Optional: leave empty to auto-pick a path (supports Railway volumes)
DATABASE_TABLE=vip_players    # Legacy option (ignored by the JSON backend)

# Announcement controls
ANNOUNCEMENT_MESSAGE_ID=

# Moderator notifications
MODERATION_CHANNEL_ID=
MODERATOR_ROLE_ID=

# Optional CRCON HTTP API integration
# Example base URL: https://crcon.example.com:8010  (the /api segment is added automatically)
CRCON_HTTP_BASE_URL=
CRCON_HTTP_BEARER_TOKEN=      # Optional: use a pre-generated token
CRCON_HTTP_USERNAME=          # Optional: if set, requires CRCON_HTTP_PASSWORD
CRCON_HTTP_PASSWORD=
CRCON_HTTP_VERIFY=true        # Optional: set to false when using self-signed certificates
CRCON_HTTP_TIMEOUT=20         # Optional: CRCON HTTP timeout in seconds

# RCON configuration
RCON_HOST=
RCON_PORT=21115
RCON_PASSWORD=
RCON_VERSION=2

